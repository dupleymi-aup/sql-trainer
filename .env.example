# ============================================
# SQL Trainer — Environment Variables
# ============================================
# Copy this file to .env.local and fill in the values.
# Never commit .env.local to version control.
# ============================================

# --------------------------------------------
# Server
# --------------------------------------------
# Optional: Override default port (auto-detects free port starting from 3000)
# PORT=3000

# Application URL (used in email links and auth callbacks)
NEXTAUTH_URL=http://localhost:3000

# --------------------------------------------
# Authentication (NextAuth.js v5)
# --------------------------------------------
# Required. Generate a secure random string:
#   openssl rand -base64 32
#   or: npx auth secret
AUTH_SECRET=your-secret-key-here

# --------------------------------------------
# Database
# --------------------------------------------
# Path to the SQLite database file for user accounts (relative to project root)
DATABASE_PATH=./data/users.db

# --------------------------------------------
# Email (SMTP — for password reset & notifications)
# --------------------------------------------
# Required for password reset emails and deadline reminders.
# Configure with any SMTP provider (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=SQL Trainer <noreply@example.com>

# --------------------------------------------
# Push Notifications (Web Push / VAPID)
# --------------------------------------------
# Required for browser push notifications (achievements, reminders).
# Generate keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:your-email@example.com

# --------------------------------------------
# Redis (optional — for distributed rate limiting in multi-server deployments)
# --------------------------------------------
# When set, rate limiting uses Redis instead of in-memory store.
# Falls back to in-memory automatically if not set.
# REDIS_URL=redis://localhost:6379

# --------------------------------------------
# External Databases (optional — for real database connections)
# --------------------------------------------
# DATABASE_URL_POSTGRESQL=postgresql://user:pass@localhost:5432/sqltrainer
# DATABASE_URL_MONGODB=mongodb://localhost:27017/sqltrainer

# --------------------------------------------
# Logging
# --------------------------------------------
# Optional: Set log verbosity. Options: error, warn, info, debug (default: info)
# LOG_LEVEL=info

# --------------------------------------------
# App Version (optional — shown in health endpoint)
# --------------------------------------------
# NEXT_PUBLIC_APP_VERSION=0.3.0

# --------------------------------------------
# Build (optional — enable bundle analyzer)
# --------------------------------------------
# Set to "true" to generate a bundle analysis report on `npm run build`
# ANALYZE=true
